import { apiKey } from './apiKey';

export const getPhotos = () => {
  // fetch(`https://api.unsplash.com/photos/?client_id=${apiKey}`)
  // .then(res => res.json())
  // .then(data => data)
  // .catch(err => err)
}